import { mkdirSync, writeFileSync } from "node:fs";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
const out=join(dirname(fileURLToPath(import.meta.url)),"..","public","data","benchmark-data.json");
const seed0=73421; let seed=seed0; const rnd=()=>{seed=(seed*1664525+1013904223)>>>0; return seed/2**32};
const round=(n,d=4)=>Number(n.toFixed(d)); const date=(y,m)=>`${y}-${String(m).padStart(2,"0")}-01`;
const companies=[['focus','Focus Brand',950000,1.78,'#111827'],['peer_a','Peer A',900000,1.62,'#2563EB'],['peer_b','Peer B',840000,1.55,'#7C3AED'],['peer_c','Peer C',760000,1.48,'#DB2777'],['peer_d','Peer D',690000,1.39,'#0F766E'],['peer_e','Peer E',610000,1.33,'#F97316'],['peer_f','Peer F',520000,1.26,'#64748B'],['peer_g','Peer G',430000,1.18,'#92400E']].map(([id,name,base,rpv,color],i)=>({id,name,base,rpv,color,i}));
const scenarios=[['base_case','Base',1],['aggressive','Upside',1.075],['conservative','Downside',0.935]];
const events=[['jan','New year promotion window',1,'medium','both'],['spring','Spring campaign window',3,'low','visits'],['midyear','Mid-year merchandising reset',6,'medium','revenue'],['routine','Back-to-routine demand shift',9,'low','both'],['peak','Major promotional peak',11,'high','both'],['holiday','Holiday demand peak',12,'high','revenue']];
const rows=[]; const hist=new Map();
function season(m){return (1+0.08*Math.sin(((m-2)/12)*Math.PI*2))*(m===11?1.18:m===12?1.24:m===1?1.08:1)*(m===7?1.06:m===8?0.92:1)}
function rowBase(c,y,m,scenario){const prev=hist.get(`${c.id}|${y-1}|${m}`); if(prev)return {visits:Math.round(prev.visits*(1.045+rnd()*0.09)*scenario[2]), revenue:Math.round(prev.revenue*(1.05+rnd()*0.1)*scenario[2])}; const v=Math.round(c.base*(1+(y-2023)*0.065+m*0.0025)*season(m)*(0.92+rnd()*0.18)); return {visits:v,revenue:Math.round(v*c.rpv*(0.96+rnd()*0.11))}}
for(let y=2023;y<=2026;y++)for(let m=1;m<=12;m++){const forecast=y===2026; const ev=events.find(e=>e[2]===m); for(const sc of forecast?scenarios:[["","",1]])for(const c of companies){const b=rowBase(c,y,m,sc); const r={date:date(y,m),period_label:date(y,m),year:y,month:m,period_type:'monthly',company_id:c.id,company_name:c.name,display_name:c.name,type:'competitor',market:'Demo Market',country:'Demo Market',segment:'Retail category',revenue:b.revenue,visits:b.visits,market_share_revenue:null,market_share_visits:null,revenue_mom_growth:null,revenue_yoy_growth:null,visits_mom_growth:null,visits_yoy_growth:null,share_revenue_change_mom:null,share_revenue_change_yoy:null,share_visits_change_mom:null,share_visits_change_yoy:null,revenue_per_visit:round(b.revenue/b.visits),revenue_share_vs_visit_share:null,rank_revenue:null,rank_visits:null,rank_share_revenue:null,rank_share_visits:null,rank_change_revenue:null,rank_change_visits:null,indexed_revenue:null,indexed_visits:null,indexed_market_share_revenue:null,has_revenue:true,has_visits:true,has_growth:false,has_share_change:false,has_efficiency:true,has_event:Boolean(ev),event_summary:ev?.[1]??null,event_names:ev?.[1]??null,strategic_signal:forecast?`${sc[1]} synthetic forecast`:'Synthetic observed sample',strategic_priority:ev?.[3]==='high'?'high':'normal',source:'Demo Connector',data_type:forecast?'forecast':'estimated',value_type:forecast?'forecast':'estimate',is_forecast:forecast,forecast_scenario:forecast?sc[0]:'',forecast_scenario_label:forecast?sc[1]:'',notes:'Randomized synthetic row for public demo use only.',active:true,company_color:c.color}; rows.push(r); if(!forecast)hist.set(`${c.id}|${y}|${m}`,r)}}
function groups(arr){const g=new Map(); for(const r of arr){const k=`${r.date}|${r.forecast_scenario||'actual'}`; if(!g.has(k))g.set(k,[]); g.get(k).push(r)} return g}
for(const rs of groups(rows).values()){const tr=rs.reduce((s,r)=>s+r.revenue,0), tv=rs.reduce((s,r)=>s+r.visits,0); for(const r of rs){r.market_share_revenue=round(r.revenue/tr,6); r.market_share_visits=round(r.visits/tv,6); r.revenue_share_vs_visit_share=round(r.market_share_revenue-r.market_share_visits,6)} for(const [m,rf] of [['revenue','rank_revenue'],['visits','rank_visits'],['market_share_revenue','rank_share_revenue'],['market_share_visits','rank_share_visits']])[...rs].sort((a,b)=>b[m]-a[m]).forEach((r,i)=>r[rf]=i+1)}
const by=new Map(); for(const r of rows.filter(r=>!r.is_forecast)){if(!by.has(r.company_id))by.set(r.company_id,[]); by.get(r.company_id).push(r)}
for(const rs of by.values()){rs.sort((a,b)=>a.date.localeCompare(b.date)); const base=rs[0]; for(let i=0;i<rs.length;i++){const r=rs[i], pm=rs[i-1], py=rs[i-12]; if(pm){r.revenue_mom_growth=round((r.revenue-pm.revenue)/pm.revenue); r.visits_mom_growth=round((r.visits-pm.visits)/pm.visits); r.share_revenue_change_mom=round(r.market_share_revenue-pm.market_share_revenue,6); r.share_visits_change_mom=round(r.market_share_visits-pm.market_share_visits,6); r.rank_change_revenue=pm.rank_revenue-r.rank_revenue; r.rank_change_visits=pm.rank_visits-r.rank_visits} if(py){r.revenue_yoy_growth=round((r.revenue-py.revenue)/py.revenue); r.visits_yoy_growth=round((r.visits-py.visits)/py.visits); r.share_revenue_change_yoy=round(r.market_share_revenue-py.market_share_revenue,6); r.share_visits_change_yoy=round(r.market_share_visits-py.market_share_visits,6)} r.indexed_revenue=round(r.revenue/base.revenue*100,2); r.indexed_visits=round(r.visits/base.visits*100,2); r.indexed_market_share_revenue=round(r.market_share_revenue/base.market_share_revenue*100,2); r.has_growth=r.revenue_mom_growth!==null||r.revenue_yoy_growth!==null; r.has_share_change=r.share_revenue_change_mom!==null||r.share_revenue_change_yoy!==null}}
for(const rs of groups(rows).values()){const s=rs[0], avg=f=>round(rs.reduce((a,r)=>a+(r[f]||0),0)/rs.length,6); rows.push({...s,company_id:'market_average',company_name:'Market Average',display_name:'Market Average',type:'benchmark',revenue:Math.round(avg('revenue')),visits:Math.round(avg('visits')),market_share_revenue:avg('market_share_revenue'),market_share_visits:avg('market_share_visits'),rank_revenue:null,rank_visits:null,rank_share_revenue:null,rank_share_visits:null,strategic_signal:s.is_forecast?'Synthetic forecast benchmark':'Synthetic market benchmark',notes:'Synthetic benchmark row generated from peer averages.',company_color:'#94A3B8'})}
const eventRows=[]; for(let y=2023;y<=2026;y++)for(const e of events)eventRows.push({event_id:`demo_${y}_${e[0]}`,date:date(y,e[2]),year:y,month:e[2],period_type:'monthly',company_id:null,scope:'market',event_type:'seasonality',event_name:e[1],note:'Generic calendar event used to demonstrate overlays.',expected_impact:e[3],metric_affected:e[4],source:'Demo Calendar',confidence:e[3]==='high'?'high':'medium',event_origin:'synthetic_calendar'});
const payload={ok:true,meta:{generated_at:new Date().toISOString(),timezone:'UTC',source:'Seeded synthetic demo data',seed:seed0,data_policy:'All brands, companies, values, events, rankings, and trends are fictional randomized samples for public portfolio use.',source_contract:'Compatible with any spreadsheet, JSON API, or database export that returns { ok: true, data: { interface: [] } }.',source_sheets:{interface:'interface',events:'events',dictionary:'dictionary'},counts:{interface:rows.length,events:eventRows.length,dictionary:4}},data:{interface:rows.sort((a,b)=>a.date.localeCompare(b.date)||a.company_id.localeCompare(b.company_id)),events:eventRows,dictionary:[{field:'company_id',label:'Company ID',description:'Stable entity identifier.'},{field:'revenue',label:'Revenue',description:'Numeric value from connected source or demo data.'},{field:'visits',label:'Visits',description:'Numeric traffic value from connected source or demo data.'},{field:'market_share_revenue',label:'Revenue share',description:'Revenue divided by total period revenue.'}]}};
mkdirSync(dirname(out),{recursive:true}); writeFileSync(out,JSON.stringify(payload,null,2)+'\n'); console.log(`Wrote ${rows.length} interface rows to ${out}`);
