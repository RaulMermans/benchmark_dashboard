# Leave empty to use /public/data/benchmark-data.json
VITE_BENCHMARK_API_URL=
VITE_CI_API_URL=
VITE_TIMESFM_API_URL=

# Set VITE_BENCHMARK_API_URL to load benchmark data from a live API
# Set VITE_TIMESFM_API_URL only for an optional advanced forecast provider.
# No env vars are required for the public static demo.
