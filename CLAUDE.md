# Project instructions

This is a public-safe benchmark intelligence dashboard/framework template.

Rules:
- Use synthetic mock data only.
- Do not add real company/client data or real logos.
- Keep calculations in `src/framework/core` where possible.
- Keep data contract docs updated when fields change.
- Run `pnpm test`, `pnpm validate:data`, and `pnpm audit:public` before release.
